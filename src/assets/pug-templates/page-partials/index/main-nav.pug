- var navBase = currentPage.name === "index" ? "" : "/"
- var navClasses = "main-nav"
- navClasses += (currentPage.name === "index") ? " fadeIn animation-1s animationDelay-4s" : ""
nav#main-nav(class=navClasses)
    a#logo.logo(href="/", title="Vanja Gavrić logo. " + $i18n.mainNav.logoTitle)
        span.circle
            | VG
        span.back
            | &larr;

    ul
        li
            a(href=navBase + "#developer", title="Developer.")
                i.icon-code-1
        li
            a(href=navBase + "#photography", title= $i18n.general.photography + ".")
                i.icon-camera
        li
            a(href=navBase + "#writing", title= $i18n.general.writing + ".")
                i.icon-feather
        li
            a(href=navBase + "#contact", title= $i18n.general.contact + ".")
                i.icon-paper-plane

#top.top-anchor
