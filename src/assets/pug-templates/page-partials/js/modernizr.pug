script(src=assetsUrl.local + "js/vendor/modernizr.js")
//- script.
    Modernizr.load([{
        // The test: does the browser understand Media Queries?
        test : Modernizr.mq("only all"),
        // If not, load the respond.js file
        nope : "#{assetsUrl.local}js/vendor/respond.min.js"
    }]);
